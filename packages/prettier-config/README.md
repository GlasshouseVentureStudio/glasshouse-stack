# `@glasshouse/prettier-config`

> A [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ pnpm add -D @glasshouse/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@glasshouse/prettier-config",
}
```
